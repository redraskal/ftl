export type ResultCardProps = {
    name: string;
    languages: string[];
    currency: string;
    timezones: string[];

};

export function ResultCard(props: ResultCardProps) {
    return (
        <>

        </>
    );
}